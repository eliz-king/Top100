<h1>New Artist</h1>

<%= form_with(model: @artist, local: true, class: 'ui form') do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: 'Artist Nmae' %>
  </div>
  <%= f.submit nil, class: 'ui blue button inverted' %>
<% end %>
